@AGENTS.md

# Code conventions

Identifiers carry the "what": name variables and functions descriptively enough
that comments explaining code stay sparse. Comments are reserved for the
non-obvious "why" — workarounds, external constraints, invariants — and kept
short.
